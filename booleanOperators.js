let a = 5;
let b = "7";

// <, >
// !=, !==, ==, ===, &&, ||

console.log(a !== b); //?
console.log(a != b); //?
console.log(a <= b); //?

// Can't change the value of const unless it is an array

let circle = 6.2;
// ceil rounds to next whole number up
console.log(Math.ceil(circle));
// floor rounds to the next whole number down
console.log(Math.floor(circle));

//Math.max -> compares values and returns the highest one
//Math.min -> compares values and returns the lowest one

//Math.pow(7,2) -> 7 to the power of 2, so this multiplies the first number by itself howmany times the second number states
